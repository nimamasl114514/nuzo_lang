# 完整命名函数测试
# 测试: 基础函数、参数、返回值、递归

fn add(a, b) {
    a + b
}

fn double(x) {
    x * 2
}

fn square(x) {
    x * x
}

fn greet(name) {
    "Hello, " + name
}

result = add(10, 20) + double(5) + square(3)
